{
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist"],
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "declarationDir": "dist",
    "types": ["node", "benchmark", "graceful-fs"]
  }
}
